// react-internal.js dùng `export default`, không có named export `config`.
import config from "@repo/eslint-config/react-internal";

/** @type {import("eslint").Linter.Config} */
export default config;
